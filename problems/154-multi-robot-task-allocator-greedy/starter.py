def allocate_tasks(robots, tasks):
    """
    Assign tasks to robots greedily based on estimated completion time.
    
    Args:
        robots (list): List of dicts {'id': int, 'x': int, 'y': int, 'available_time': float}
        tasks (list): List of dicts {'id': int, 'x': int, 'y': int, 'duration': float}
        
    Returns:
        dict: Mapping {task_id: robot_id}
    """
    # TODO: Calculate costs (travel time + wait time)
    # TODO: Sort pairs or find minimum cost assignment greedily
    # TODO: Update robot availability after assignment
    # TODO: Return assignment map
    pass
